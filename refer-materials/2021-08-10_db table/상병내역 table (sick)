create table sick(
	clinic_no integer,
	category_main_no integer
	
)
alter table sick
ADD constraint clinic_no
foreign key (clinic_no)
references clinic(no)

alter table sick
ADD constraint category_main_no
foreign key(category_main_no)
references category_main(no)